<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Default Queue Connection Name
    |--------------------------------------------------------------------------
    |
    | Laravel queue в этом сервисе не используется как механизм доставки
    | работ — за это отвечает свой consumer на php-amqplib с очередями
    | RabbitMQ. Дефолт sync означает: если Laravel-ядро или сторонний пакет
    | внутри что-то диспатчит через Bus::dispatch(), задача выполнится
    | синхронно в том же процессе.
    |
    */

    'default' => env('QUEUE_CONNECTION', 'sync'),

    /*
    |--------------------------------------------------------------------------
    | Queue Connections
    |--------------------------------------------------------------------------
    */

    'connections' => [

        'sync' => [
            'driver' => 'sync',
        ],

        'null' => [
            'driver' => 'null',
        ],

    ],

    /*
    |--------------------------------------------------------------------------
    | Job Batching
    |--------------------------------------------------------------------------
    |
    | Batching и Failed Jobs не используем (свой consumer не интегрирован
    | с Laravel Bus). Секции оставлены для совместимости с
    | QueueServiceProvider — без них TypeError при бутстрапе.
    |
    */

    'batching' => [
        'database' => env('DB_CONNECTION', 'pgsql'),
        'table' => 'job_batches',
    ],

    'failed' => [
        'driver' => 'null',
    ],

];
