<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Default Filesystem Disk
    |--------------------------------------------------------------------------
    |
    | В этом сервисе файловый storage не используется (нет загрузок файлов, нет
    | публичной статики, логи идут в stderr). Disk local оставлен только
    | для совместимости с Laravel — некоторые core-сервисы при бутстрапе
    | требуют наличия default-диска.
    |
    */

    'default' => env('FILESYSTEM_DISK', 'local'),

    /*
    |--------------------------------------------------------------------------
    | Filesystem Disks
    |--------------------------------------------------------------------------
    */

    'disks' => [

        'local' => [
            'driver' => 'local',
            'root' => storage_path('app/private'),
            'throw' => false,
            'report' => false,
        ],
    ],
];
